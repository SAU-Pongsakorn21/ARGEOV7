<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AddActivity">
        <item name="android:textSize">@dimen/detail_text_size_18sp</item>
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">@dimen/view_margin_16dp</item>
        <item name="android:layout_marginLeft">@dimen/view_margin_16dp</item>
        <item name="android:layout_marginBottom">@dimen/view_margin_16dp</item>
        <item name="android:gravity">left|center_vertical</item>
    </style>

    <style name="lnl_AddActivity">
        <item name="android:orientation">horizontal</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:layout_marginTop">@dimen/view_margin_16dp</item>
        <item name="android:layout_marginLeft">@dimen/view_margin_16dp</item>
        <item name="android:layout_marginRight">@dimen/view_margin_16dp</item>
    </style>

    <style name="edt_AddActivity">
        <item name="android:layout_margin">@dimen/view_margin_16dp</item>
        <item name="android:padding">4dp</item>
        <item name="android:background">@color/colorWhite</item>
    </style>
</resources>
